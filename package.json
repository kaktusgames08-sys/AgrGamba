{
  "name": "kolo-nestesti",
  "version": "3.0.0",
  "description": "Kolo neštěstí — configurable high-energy browser wheel for GitHub Pages and OBS.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm test && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
