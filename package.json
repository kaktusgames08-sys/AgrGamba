{
  "name": "gamba",
  "version": "1.0.0",
  "description": "GAMBA — Agraelus vs Chatter meme randomizer. No real gambling, no money, just a dopamine-cannon slot machine that decides who takes the L.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm test && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
