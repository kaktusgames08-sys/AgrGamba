{
  "name": "kolo-nestesti",
  "version": "4.2.0",
  "description": "Kolo neštěstí — premium casino-style browser wheel with readable leaderboard, 7TV amount emote, piggy-bank finale and fair random landings.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm test && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
