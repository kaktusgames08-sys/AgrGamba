{
  "name": "kolo-nestesti",
  "version": "4.1.0",
  "description": "Kolo neštěstí — smooth browser wheel with adaptive fullscreen UI, piggy-bank finale and fair random landings.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm test && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
