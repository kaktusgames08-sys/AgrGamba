{
  "name": "kolo-nestesti",
  "version": "4.2.1",
  "description": "Kolo neštěstí — premium browser wheel with neutral wheel staging, compact HUD, growing piggy bank and fair random landings.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build": "npm test && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
